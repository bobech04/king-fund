# King Fund — dépendances Python
# Généré le 2026-06-12

# --- Serveur web ---
flask>=3.0.0
flask-cors>=4.0.0
flask-sock>=0.7.0
simple-websocket>=1.0.0

# --- Serveur WSGI production (Linux/RPi) ---
# Lancer avec : gunicorn -k geventwebsocket.gunicorn.workers.GeventWebSocketWorker -w 1 --bind 0.0.0.0:5000 app:app
gunicorn>=21.2.0
gevent>=23.9.1
gevent-websocket>=0.10.1

# --- Serveur WSGI production (Windows) ---
# Lancer avec : start_production.bat  (ou : waitress-serve --port=5000 app:app)
waitress>=3.0.0

# --- Planificateur ---
apscheduler>=3.10.0
pytz>=2024.1

# --- Données marché ---
yfinance>=0.2.40
pandas>=2.0.0
fredapi>=0.5.2

# --- HTTP / réseau ---
requests>=2.31.0
aiohttp>=3.9.0

# --- IA ---
anthropic>=0.34.0

# --- Divers ---
python-dotenv>=1.0.0
fpdf2>=2.7.9
rich>=13.7.0
